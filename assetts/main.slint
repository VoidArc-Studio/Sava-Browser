import { Theme } from "styles.slint";

export component AppWindow inherits Window {
    width: 1280px;
    height: 720px;
    title: "Sava Browser";
    background: AppTheme.current-theme.background;

    // Pasek boczny
    Rectangle {
        width: 60px;
        height: parent.height;
        background: AppTheme.current-theme.secondary-color;
        border-radius: AppTheme.current-theme.border-radius;
        drop-shadow: 0px 2px 5px AppTheme.current-theme.shadow-color;

        VerticalLayout {
            spacing: 10px;
            padding: 10px;
            Image {
                source: @image-url("icons/bookmarks.svg");
                width: 40px;
                height: 40px;
                clicked => { bookmarks_popup.visible = true; }
            }
            Image {
                source: @image-url("icons/history.svg");
                width: 40px;
                height: 40px;
                clicked => { history_popup.visible = true; }
            }
            Image {
                source: @image-url("icons/settings.svg");
                width: 40px;
                height: 40px;
                clicked => { settings_popup.visible = true; }
            }
            Image {
                source: @image-url("icons/plugins.svg");
                width: 40px;
                height: 40px;
                clicked => { plugins_popup.visible = true; }
            }
        }
    }

    // Pasek kart i adresu
    Rectangle {
        x: 60px;
        width: parent.width - 60px;
        height: 60px;
        background: AppTheme.current-theme.primary-color;
        border-radius: AppTheme.current-theme.border-radius;
        drop-shadow: 0px 2px 5px AppTheme.current-theme.shadow-color;

        HorizontalLayout {
            spacing: 8px;
            padding: 8px;

            // Przycisk nowej karty
            Button {
                text: "+";
                width: 40px;
                height: 40px;
                background: AppTheme.current-theme.accent-color;
                border-radius: AppTheme.current-theme.border-radius;
                clicked => { root.new_tab(); }
                mouse-cursor: pointer;
            }

            // Dynamiczne karty
            for tab in ["Tab 1", "Tab 2"]: Rectangle {
                width: 150px;
                height: 40px;
                background: AppTheme.current-theme.secondary-color;
                border-radius: AppTheme.current-theme.border-radius;
                drop-shadow: 0px 1px 3px AppTheme.current-theme.shadow-color;
                HorizontalLayout {
                    Text {
                        text: tab;
                        color: AppTheme.current-theme.text-color;
                        vertical-alignment: center;
                        horizontal-alignment: center;
                        font-size: 14px;
                    }
                    Button {
                        text: "x";
                        width: 20px;
                        height: 20px;
                        background: transparent;
                        color: AppTheme.current-theme.text-color;
                        clicked => { root.close_tab(index); }
                    }
                }
                TouchArea {
                    clicked => { root.switch_tab(index); }
                    mouse-cursor: pointer;
                    mouse-entered => { background = AppTheme.current-theme.hover-color; }
                }
            }

            // Pasek adresu
            Rectangle {
                width: parent.width - 350px;
                height: 40px;
                background: AppTheme.current-theme.background;
                border-radius: AppTheme.current-theme.border-radius;
                border-width: 1px;
                border-color: #dadce0;
                drop-shadow: 0px 1px 2px AppTheme.current-theme.shadow-color;

                TextInput {
                    id: url_bar;
                    placeholder-text: "Search or enter URL";
                    width: parent.width - 10px;
                    height: parent.height - 10px;
                    font-size: 16px;
                    color: AppTheme.current-theme.text-color;
                    accepted => { root.open_url(url_bar.text); }
                }
            }

            // Przyciski
            HorizontalLayout {
                spacing: 5px;
                Button {
                    text: "AI";
                    width: 50px;
                    height: 40px;
                    background: AppTheme.current-theme.accent-color;
                    border-radius: AppTheme.current-theme.border-radius;
                    clicked => { ai_popup.visible = true; }
                    mouse-cursor: pointer;
                }
                Button {
                    text: "Incognito";
                    width: 80px;
                    height: 40px;
                    background: AppTheme.current-theme.accent-color;
                    border-radius: AppTheme.current-theme.border-radius;
                    clicked => { root.toggle_incognito(); }
                    mouse-cursor: pointer;
                }
                Button {
                    text: "☆";
                    width: 40px;
                    height: 40px;
                    background: AppTheme.current-theme.accent-color;
                    border-radius: AppTheme.current-theme.border-radius;
                    clicked => { root.add_bookmark(url_bar.text); }
                    mouse-cursor: pointer;
                }
                Button {
                    text: "☰";
                    width: 40px;
                    height: 40px;
                    background: AppTheme.current-theme.accent-color;
                    border-radius: AppTheme.current-theme.border-radius;
                    clicked => { menu_popup.visible = true; }
                    mouse-cursor: pointer;
                }
            }
        }
    }

    // Obszar WebView
    Rectangle {
        x: 60px;
        y: 60px;
        width: parent.width - 60px;
        height: parent.height - 60px;
        background: #ffffff;
        // TODO: Osadzenie WebView
    }

    // Popup asystenta AI
    PopupWindow {
        id: ai_popup;
        width: 400px;
        height: 350px;
        x: parent.width - 410px;
        y: 70px;
        visible: false;
        background: AppTheme.current-theme.background;
        border-radius: AppTheme.current-theme.border-radius;
        drop-shadow: 0px 2px 5px AppTheme.current-theme.shadow-color;

        VerticalLayout {
            spacing: 10px;
            padding: 10px;
            Text {
                text: "Sava AI Assistant";
                font-size: 18px;
                color: AppTheme.current-theme.text-color;
            }
            ComboBox {
                id: ai_mode;
                model: ["Conversation", "Web Search", "Developer Mode"];
                current-index: 0;
                width: parent.width - 20px;
            }
            TextInput {
                id: ai_query;
                placeholder-text: "Ask Sava AI...";
                width: parent.width - 20px;
                font-size: 14px;
            }
            Button {
                text: "Send";
                background: AppTheme.current-theme.primary-color;
                color: AppTheme.current-theme.text-color;
                border-radius: AppTheme.current-theme.border-radius;
                clicked => {
                    root.ai_request(ai_query.text, ai_mode.current-index);
                    ai_popup.visible = false;
                }
                mouse-cursor: pointer;
            }
        }
    }

    // Menu ustawień
    PopupWindow {
        id: menu_popup;
        width: 220px;
        height: 350px;
        x: parent.width - 230px;
        y: 70px;
        visible: false;
        background: AppTheme.current-theme.background;
        border-radius: AppTheme.current-theme.border-radius;
        drop-shadow: 0px 2px 5px AppTheme.current-theme.shadow-color;

        VerticalLayout {
            spacing: 5px;
            padding: 5px;
            Button {
                text: "Settings";
                clicked => { settings_popup.visible = true; }
            }
            Button {
                text: "View Bookmarks";
                clicked => { bookmarks_popup.visible = true; }
            }
            Button {
                text: "View History";
                clicked => { history_popup.visible = true; }
            }
            Button {
                text: "Clear Cookies";
                clicked => { /* TODO: Wyczyść ciasteczka */ }
            }
            Button {
                text: "Manage Plugins";
                clicked => { plugins_popup.visible = true; }
            }
            Button {
                text: "Toggle Dark Mode";
                clicked => { AppTheme.change-theme("dark"); }
            }
            Button {
                text: "Exit";
                clicked => { root.close(); }
            }
        }
    }

    // Popup zakładek
    PopupWindow {
        id: bookmarks_popup;
        width: 300px;
        height: 400px;
        x: 70px;
        y: 70px;
        visible: false;
        background: AppTheme.current-theme.background;
        border-radius: AppTheme.current-theme.border-radius;
        drop-shadow: 0px 2px 5px AppTheme.current-theme.shadow-color;

        VerticalLayout {
            spacing: 5px;
            padding: 5px;
            Text {
                text: "Bookmarks";
                font-size: 18px;
                color: AppTheme.current-theme.text-color;
            }
            // TODO: Dynamiczna lista zakładek
            for bookmark in ["Bookmark 1", "Bookmark 2"]: Text {
                text: bookmark;
                color: AppTheme.current-theme.text-color;
                clicked => { root.open_url(bookmark); }
                mouse-cursor: pointer;
            }
        }
    }

    // Popup historii
    PopupWindow {
        id: history_popup;
        width: 300px;
        height: 400px;
        x: 70px;
        y: 70px;
        visible: false;
        background: AppTheme.current-theme.background;
        border-radius: AppTheme.current-theme.border-radius;
        drop-shadow: 0px 2px 5px AppTheme.current-theme.shadow-color;

        VerticalLayout {
            spacing: 5px;
            padding: 5px;
            Text {
                text: "History";
                font-size: 18px;
                color: AppTheme.current-theme.text-color;
            }
            // TODO: Dynamiczna lista historii
            for history in ["History 1", "History 2"]: Text {
                text: history;
                color: AppTheme.current-theme.text-color;
                clicked => { root.open_url(history); }
                mouse-cursor: pointer;
            }
        }
    }

    // Popup wtyczek
    PopupWindow {
        id: plugins_popup;
        width: 300px;
        height: 400px;
        x: 70px;
        y: 70px;
        visible: false;
        background: AppTheme.current-theme.background;
        border-radius: AppTheme.current-theme.border-radius;
        drop-shadow: 0px 2px 5px AppTheme.current-theme.shadow-color;

        VerticalLayout {
            spacing: 5px;
            padding: 5px;
            Text {
                text: "Plugins";
                font-size: 18px;
                color: AppTheme.current-theme.text-color;
            }
            // TODO: Dynamiczna lista wtyczek
        }
    }

    // Popup ustawień
    PopupWindow {
        id: settings_popup;
        width: 400px;
        height: 500px;
        x: 70px;
        y: 70px;
        visible: false;
        background: AppTheme.current-theme.background;
        border-radius: AppTheme.current-theme.border-radius;
        drop-shadow: 0px 2px 5px AppTheme.current-theme.shadow-color;

        VerticalLayout {
            spacing: 10px;
            padding: 10px;
            Text {
                text: "Settings";
                font-size: 18px;
                color: AppTheme.current-theme.text-color;
            }
            CheckBox {
                id: block_ads;
                text: "Block Ads";
                checked: true;
            }
            CheckBox {
                id: block_js;
                text: "Block JavaScript";
                checked: false;
            }
            CheckBox {
                id: block_fingerprinting;
                text: "Block Fingerprinting";
                checked: true;
            }
            Text {
                text: "Homepage";
                color: AppTheme.current-theme.text-color;
            }
            TextInput {
                id: homepage;
                placeholder-text: "Enter homepage URL";
                width: parent.width - 20px;
                text: "https://startpage.com";
            }
            Text {
                text: "Theme";
                color: AppTheme.current-theme.text-color;
            }
            ComboBox {
                id: theme;
                model: ["Light", "Dark", "Custom"];
                current-index: 0;
                width: parent.width - 20px;
            }
            Button {
                text: "Save";
                background: AppTheme.current-theme.primary-color;
                color: AppTheme.current-theme.text-color;
                border-radius: AppTheme.current-theme.border-radius;
                clicked => {
                    root.save_settings(theme.current-value, homepage.text, block_ads.checked, block_js.checked);
                    settings_popup.visible = false;
                }
            }
        }
    }

    callback open_url(string);
    callback new_tab();
    callback close_tab(int);
    callback switch_tab(int);
    callback toggle_incognito();
    callback add_bookmark(string);
    callback ai_request(string, int);
    callback save_settings(string, string, bool, bool);
}
